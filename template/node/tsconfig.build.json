{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // Only the build config pins rootDir: the base config also type-checks
    // tests/, which live outside src/.
    "rootDir": "src"
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist", "tests", "**/*.test.ts"]
}
